let i = 0;

while ( i <= 10 ) {
    console.log(i); // Log `i` before incrementing
    i += 2;         // increment `i` by 2 after logging
}

i = 5;

while (i >= 0) {
    console.log(i); // Log `i` before decrementing
    i--;            // Decrement `i` by 1 after loggings
}